<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MyApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MyApp.Splash" parent="Theme.SplashScreen">
        <!-- Set your background color -->
        <item name="windowSplashScreenBackground">@color/myGreen</item>
        <!-- Set your logo drawable -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/app_logo</item>
        <item name="windowSplashScreenAnimationDuration">2000</item>
        <item name="postSplashScreenTheme">@style/Theme.MyApp</item>

        <item name="android:statusBarColor" tools:targetApi="l">@color/myGreen</item>
        <item name="android:navigationBarColor">@color/myGreen</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>


    <style name="Theme.App" parent="Base.Theme.BIT603_Mitchell_Travis_5080526_AS2" />
</resources>